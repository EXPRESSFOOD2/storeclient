
const initialstate = {
    //NIY STATES

}

const rootReducer = (state = initialState, action) =>{
    //NIY KEY'S
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}

export default rootReducer;